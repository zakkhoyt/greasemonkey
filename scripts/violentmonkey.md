
# TODO: zakkhoyt P1 - Open webpage (that the script affects), in a browser. Lets' support this in a nummber of ways
# Read the meta lines from user.js looking for all lines that match:
# * `// @match        <parse this>`
# * `// @include      <parse this>`
# Real examples
# * `// @match        *://*/*`
# * `// @include      *://*.github.com/*`